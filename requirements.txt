# EMERALD-AI — runtime dependencies (MSc scope). Pin majors; tighten before submission.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1        # read the .xlsx dataset
shap>=0.44           # Phase 4 explainability
mapie>=0.8           # Phase 4 split-conformal (Core-light)
fastapi>=0.110       # Phase 5 decision-support demo API
uvicorn>=0.27        # Phase 5 demo server
requests>=2.31       # research_bot OpenAlex client
PyYAML>=6.0          # research_bot literature brain
pytest>=7.4          # tests
