# Seed queries for the literature crawler — aimed squarely at the dissertation's problem:
# methods for EXTREME-IMBALANCE green-loan credit scoring. Grouped by theme for readability.
queries:
  imbalance_methods:
    - "class imbalance credit scoring"
    - "imbalanced classification rare events"
    - "SMOTE credit scoring oversampling"
    - "cost-sensitive learning credit default"
    - "focal loss imbalanced tabular"
    - "anomaly detection rare default prediction"
  calibration_uncertainty:
    - "probability calibration credit risk"
    - "conformal prediction credit scoring"
    - "uncertainty quantification imbalanced classification"
  green_finance:
    - "green loan credit risk machine learning"
    - "sustainable finance default prediction"
    - "ESG credit scoring machine learning"
  explainability_fairness:
    - "SHAP explainability credit scoring"
    - "counterfactual explanation credit decision"
    - "fairness credit scoring imbalanced"
  selection_bias:
    - "reject inference credit scoring"
    - "sample selection bias accepted applicants credit"
  tabular_models:
    - "gradient boosting credit default tabular"
    - "deep learning tabular credit scoring benchmark"
  # Added 2026-06-10 to close methodology-justification gaps (D2/D3/D4/D7) found in the audit.
  methodology_justification:
    - "resampling within cross-validation folds data leakage"          # D2
    - "cross-validation imbalanced data overfitting overoptimism"      # D2
    - "precision recall curve imbalanced classification evaluation"    # D3
    - "ROC versus precision recall imbalanced datasets"               # D3
    - "evaluation metrics credit scoring imbalanced"                  # D3
    - "repeated stratified k-fold cross-validation variance"          # D4
    - "events per variable logistic regression"                       # D7
    - "minimum sample size clinical prediction model"                 # D7
    - "Platt scaling probability calibration classifier"              # D6
    - "isotonic regression probability calibration"                   # D6
    - "predicting good probabilities supervised learning calibration" # D6
  # Added 2026-06-29 to close the RQ1-follow-up / decision-layer gaps (D10-D14).
  followup_justification:
    - "regression shrinkage and selection via the lasso"             # D10 LASSO
    - "regularization variable selection elastic net"                # D10 elastic-net
    - "financial ratios discriminant analysis corporate bankruptcy"  # D11 financial ratios
    - "feature selection small sample class imbalance"               # D12 feature selection
    - "Cox regression models life-tables survival"                   # D13 survival
    - "foundations of cost-sensitive learning threshold"             # D14 cost threshold
